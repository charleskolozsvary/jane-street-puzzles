# Dependencies (from what I remember)
```shell
conda create -name shutbox python=3.14
conda activate shutbox
pip install pymupdf
pip install imageio
brew install ffmpeg
pip install imageio-ffmpeg
```


